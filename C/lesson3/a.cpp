#include <bits/stdc++.h>

using namespace std;

int main(){
    
    int n;
    cin >> n;
    int massive[n];
    int massive2[n];
    for(int i = 0; i < n; i++){
        cin >> massive[i];
        massive2[i] = massive[i];
    }
    
    // sort(massive, massive + n);
        // 0               n

    // for(int i = n - 1; i >= 0; i--){
    //     cout << massive[i] << " ";
    // }

    // reverse(massive, massive + n);
    // // reverse - переверни
    // for(int i = 0; i < n; i++){
    //     cout << massive[i] << " ";
    // }
    // 1 1 2 2 
    
}