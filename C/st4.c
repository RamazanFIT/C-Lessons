#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

int main() {

    // bitwise operations 

    // and or xor сдвиг вправо сдвиг влево 

    // true and true = true 
    // true and false = false
    // true or false = true 

    // printf("%d", (1 & 1)); and 
    // printf("%d", (0 | 1)); or 
    // printf("%d", (0 ^ 1)); xor 
    // 1010101
    // 0101111  xor 
    // 1111010 -> decimal 10
    // printf("%d", (1 << 10)); 
    // printf("%d", (1024 >> 10)); 

    // (1 << 10) = 2 ** 10 
    // 1 = 000000001
    // 1 = 000000010
    // 1 = 100000000
    // (1024 >> 10) = 2 ** 10 
    // 1 = 100000000
    // 1 = 000000010
    // 1 = 000000001




    // && = and 
    // || = or not in bitwise operations


    // if(a == 1 && b == 2)


    return 0;
}