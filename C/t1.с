#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

struct pair{
    int name;
    int age;
};

typedef struct {
    char name[50];
    int age;
    float height;
} Person;


int main() {

    // struct pair a = {1, 2};
    // // a.name = 12;
    // // a.age = 1;
    // printf("name : %d  age: %d", a.name, a.age);

    return 0;
}