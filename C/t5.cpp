#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

int main() {
	
  	// const int n = 5;
    // int n;
    // int massive[n] = {1, 2, 3, 4, 5};

    // int * pointer = massive;
    // for(int i = 0; i < n; i++){
    //     // printf("%d ", pointer[i]);
    //     printf("%d ", *pointer + i);
      
    // }
    // я хочу вывести массив через pointer 
  // [] нельзя 
 	 // pointer[x] = *(pointer + x)
    // int n;
    // scanf("%d", &n);
    // int massive[n];
    return 0;
}
